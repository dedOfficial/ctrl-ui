pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx,mjs,cjs}"
      run: pnpm exec oxlint {staged_files}
    fmt:
      glob: "*.{js,jsx,ts,tsx,mjs,cjs,json,jsonc,md,yml,css}"
      run: pnpm exec oxfmt --check {staged_files}

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
